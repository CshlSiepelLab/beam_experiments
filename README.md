# A bayesian approach to inferring anatomical tissue locations for internal nodes in a cancer phylogeny with labeled leaves.

Most inference runs on simulated or real datasets can be done using the snakemake pipelines in `scripts/pipelines/` and `scripts/` otherwise contains some dependency formatting and plotting scripts.