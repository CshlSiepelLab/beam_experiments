# Pipelines and scripts used for analyses in the BEAM method paper.

Most inference runs on simulated or real datasets can be done using the snakemake pipelines in `pipelines/`. Pipeline dependencies are in `inputs/` and `scripts/`. The `scripts/` directory also contains some standalone scripts.
