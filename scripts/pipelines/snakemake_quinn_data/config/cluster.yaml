__default__:
  cores: '{params.threads}'
  mem: '{params.mem}'
  logout: '{outdir}/job_logs/{wildcards.mouse}/{wildcards.cp}/{rule}.out'
  logerror: '{outdir}/job_logs/{wildcards.mouse}/{wildcards.cp}/{rule}.err'
  runtime: 24:00:00  # hr:min:sec

convertMatrixToSuccessive:
    logout: '{outdir}/job_logs/{wildcards.mouse}/{rule}.out'
    logerror: '{outdir}/job_logs/{wildcards.mouse}/{rule}.err'

  