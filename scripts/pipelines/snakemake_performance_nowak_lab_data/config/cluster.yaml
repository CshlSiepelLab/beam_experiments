__default__:
  cores: '{params.threads}'
  mem: '5G'
  logout: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.out'
  logerror: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.err'
  runtime: '72:00:00'  # timelimit for each job in hours:minutes:seconds

  