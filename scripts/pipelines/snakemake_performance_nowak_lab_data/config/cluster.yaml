__default__:
  cores: '{params.threads}'
  mem: '5G'
  logout: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{wildcards.cp}/{rule}.out'
  logerror: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{wildcards.cp}/{rule}.err'
  runtime: '120:00:00'  # timelimit for each job in hours:minutes:seconds

runEvotracer:
  logout: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.out'
  logerror: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.err'

plotEvotracerResults:
  logout: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.out'
  logerror: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.err'

splitCPs:
  logout: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.out'
  logerror: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.err'

plotCpNumAsvs:
  logout: '{wildcards.outdir}/job_logs/{rule}.out'
  logerror: '{wildcards.outdir}/job_logs/{rule}.err'

runBeam:
  runtime: '120:00:00'

summarizeMachina:
  logout: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.out'
  logerror: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.err'

plotMachina:
  logout: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.out'
  logerror: '{wildcards.outdir}/job_logs/{wildcards.mouse}/{rule}.err'

runMetient:
  mem: '5G'

  